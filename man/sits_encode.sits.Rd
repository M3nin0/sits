% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sits_encode.R
\name{sits_encode.sits}
\alias{sits_encode.sits}
\title{Encode a set of time series}
\usage{
\method{sits_encode}{sits}(
  data,
  encoder,
  ...,
  impute_fn = impute_linear(),
  multicores = 2L,
  gpu_memory = 4L,
  batch_size = 2L^15L,
  progress = TRUE
)
}
\arguments{
\item{data}{Set of time series (tibble of class \code{"sits"}).}

\item{encoder}{Encoder closure trained by
\code{\link[sits]{sits_pre_train}} (class \code{"sits_encoder"}).}

\item{...}{Additional arguments passed to lower-level encoding
routines.}

\item{impute_fn}{Imputation function used to interpolate missing
values in each time series (default:
\code{\link[sits]{impute_linear}}).}

\item{multicores}{Integer. Number of CPU cores used for processing
(minimum 1, maximum 2048).}

\item{gpu_memory}{Deprecated and no longer used - GPU memory is bounded
by \code{batch_size}.}

\item{batch_size}{Integer. Number of rows sent to the GPU in each forward
pass (default = 32768).}

\item{progress}{Logical. If \code{TRUE}, show a progress bar.}
}
\value{
A tibble of class \code{"sits"} with \code{time_series} containing the
embeddings produced by \code{encoder}.
}
\description{
Encodes a \pkg{sits} time-series tibble using a pre-trained deep
learning encoder returned by \code{\link[sits]{sits_pre_train}}.

This method is dispatched when \code{data} is a tibble of class
\code{"sits"}. The output preserves the input structure and replaces
the \code{time_series} column with the corresponding embeddings.
}
\note{
The \code{impute_fn} parameter defines a one-dimensional imputation
function used to interpolate missing values. Users may provide custom
functions that follow the same contract.

The encoding backend is selected automatically. When GPU execution is
available and the encoder supports it, \code{batch_size} controls how
many rows are sent to the GPU in each forward pass, bounding the GPU
memory used. Do not confuse it with the \code{batch_size} used to train
an encoder, which is much smaller. It is recommended to leave at least
1 GB of free GPU memory for model and runtime overhead.

On systems with unified CPU/GPU memory (e.g., Apple M-series), a
conservative \code{batch_size} may be required to avoid memory
contention.
}
\examples{
if (sits_run_examples()) {
    # Pre-train an encoder and encode a subset of samples
    enc <- sits_pre_train(
        samples = samples_modis_ndvi,
        encoder_method = sits_ssl_mae(mask_ratio = 0.5)
    )

    point_ndvi <- sits_select(point_mt_6bands, bands = c("NDVI"))
    point_emb <- sits_encode(data = point_ndvi, encoder = enc)
    plot(point_emb)
}

}
\author{
Alexandre Assuncao, \email{alexcarssuncao@gmail.com}

Rolf Simoes, \email{rolfsimoes@gmail.com}
}
